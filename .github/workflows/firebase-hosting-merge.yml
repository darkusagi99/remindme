# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
  workflow_dispatch:
env:
  FIREBASE_CONF : $ {{ env.FIREBASE_CONFIG }}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cat ./.github/workflows/firebase-hosting-merge.yml
      - run: echo $FIREBASE_CONF
      - run: sed -i 's\##FIREBASE_CONFIG##\$FIREBASE_CONF\g' ./.github/workflows/firebase-hosting-merge.yml
      - run: cat ./.github/workflows/firebase-hosting-merge.yml
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_REMINDME_6BFA7 }}'
          channelId: live
          projectId: remindme-6bfa7
